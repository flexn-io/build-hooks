describe('Test template', () => {
    beforeEach(() => {
        console.log('Testing');
    });
    it('init test', async () => {
        const x = true;
        expect(x).toEqual(true);
    });
});
